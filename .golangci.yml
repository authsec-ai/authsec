run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - bodyclose
    - sqlclosecheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 50
  max-same-issues: 5
